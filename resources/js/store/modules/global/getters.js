export default {
    // majorData: state => index => state.majorCards[index].majorData
    locationPoints: state => state.locationPoints,
    photoId: state => index  =>
        state.locationsPoints[index].id
    //  state.locationPoints[Object.keys(state.locationPoints).length].id
};
